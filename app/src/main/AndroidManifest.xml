<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Currentdonordetails"></activity>
        <activity android:name=".Currentreceiverdetails" />
        <activity android:name=".Currentstatus" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".Donordetails" />
        <activity android:name=".receiverfeed" />
        <activity android:name=".CreateNewAccount" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".volunteerform" />
        <activity android:name=".receiverform" />
        <activity android:name=".donationform" />
        <activity
            android:name=".mainfeed"
            android:label="@string/title_activity_mainfeed"
            android:theme="@style/Theme.Login.NoActionBar" />
        <activity android:name=".manageotp" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>